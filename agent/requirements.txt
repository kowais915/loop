# LOOP agent — FastAPI brain
# fastapi>=0.136 needed for compatibility with the starlette the mcp SDK pulls.
fastapi==0.136.3
uvicorn[standard]==0.34.0
pydantic==2.13.4          # mcp 1.27 requires >=2.11
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# MCP client (streamable-HTTP transport to the Splunk MCP Server)
# Needs >=1.8 for mcp.client.streamable_http + httpx_client_factory.
mcp==1.27.2

# Supabase (state + realtime + pgvector)
supabase==2.11.0

# Hosted-model layer (OpenAI-compatible client for Foundation-Sec-8B)
openai==1.59.6

# Local embeddings for incident-signature memory (swappable)
sentence-transformers==3.3.1
